{
  "name": "agentzon-web",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild chain.js --bundle --format=iife --minify --outfile=../site/js/agentzon-chain.js --define:global=globalThis --loader:.json=json"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.31.1",
    "@solana/spl-token": "^0.4.9",
    "buffer": "^6.0.3"
  },
  "devDependencies": {
    "esbuild": "^0.24.2"
  },
  "overrides": { "uuid": "9.0.1" }
}
