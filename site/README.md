
# AGENTZON site (frontend)
Deployed at https://agentzon.xyz (DO box /root/agentzon/site, nginx). Talks to the backend API (../app) at /api/* which reads the devnet programs. 'Live on Devnet' section = real on-chain data. Images (logo/banner/favicons) live on the server, not committed here.
